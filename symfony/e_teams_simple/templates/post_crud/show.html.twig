{% extends 'base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
    <h1>Post</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ post.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ post.content }}</td>
            </tr>
            <tr>
                <th>Said</th>
                <td>{{ post.said ? post.said|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('post_crud_index') }}">back to list</a>

    <a href="{{ path('post_crud_edit', {'id': post.id}) }}">edit</a>

    {{ include('post_crud/_delete_form.html.twig') }}
{% endblock %}
