{% extends 'base.html.twig' %}

{% block title %}Tryst{% endblock %}

{% block body %}
    <h1>Tryst</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tryst.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tryst.name }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ tryst.title }}</td>
            </tr>
            <tr>
                <th>Topic</th>
                <td>{{ tryst.topic }}</td>
            </tr>
            <tr>
                <th>Day</th>
                <td>{{ tryst.day ? tryst.day|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Hour</th>
                <td>{{ tryst.hour ? tryst.hour|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tryst_crud_index') }}">back to list</a>

    <a href="{{ path('tryst_crud_edit', {'id': tryst.id}) }}">edit</a>

    {{ include('tryst_crud/_delete_form.html.twig') }}
{% endblock %}
